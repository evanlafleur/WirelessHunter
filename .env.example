# Copy this file to .env and edit it - .env is gitignored so your own
# SSIDs never get committed.
#
# Comma-separated, exact-match SSID names to hide from the list when
# "hide mode" is toggled on (hold UP+DOWN together for 3 seconds).
HIDDEN_SSIDS=MyHomeNetwork,MyOtherNetwork
